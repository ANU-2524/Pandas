# Pandas

Day - 1 :</br>
`.head() -> to return rows from start.`</br>

`.tail() -> to return rows from end.`</br>

`.set_index() -> set a column as index.`</br>

`.reset_index() -> remove the index property from column.`</br>

`.to_excel() -> make the dataset into the spreadsheet.`</br>

`.columns : return columns name.`</br>
